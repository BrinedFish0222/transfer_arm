import 'package:flutter/material.dart';

class game_script_run_model extends StatelessWidget {
  const game_script_run_model({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("game_script_run_model page"),);
  }
}
